const student = {
    stId : 01,
    stName : "Arif",
    stMajor : "Math",
    isAReularStudent : true
};

const studentKey = Object.keys;
const studentValue = Object.values;
const studentEntries = Object.entries;

console.log(student.stMajor);
